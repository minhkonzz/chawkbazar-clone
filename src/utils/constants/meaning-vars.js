export const INCREASE_ONCE = 1;
export const DECREASE_ONCE = -1;
export const MOBILE_VIEW = window.innerWidth < 740; 
export const TABLET_VIEW = window.innerWidth >= 740 && window.innerWidth <= 1023; 
export const PC_VIEW = window.innerWidth > 1023;